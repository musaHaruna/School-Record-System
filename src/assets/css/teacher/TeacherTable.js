import styled from 'styled-components'

const Wrapper = styled.article`
  .teachers-table-filters .filter {
    display: flex;
    justify-content: start;
    font-size: 12px;
  }
`

export default Wrapper
